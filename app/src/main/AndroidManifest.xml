<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atfalna.atfalna">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme_NoActionBar">

        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Home2_Activity"
            android:theme="@style/AppTheme_NoActionBar_home"/>

        <activity
            android:name=".Login2_Activity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme_NoActionBar2" />

        <activity android:name=".Registration2_Activity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme_NoActionBar2"></activity>
    </application>

</manifest>